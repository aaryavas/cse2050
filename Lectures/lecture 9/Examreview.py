'Linked list and nodes'#<------
'big(O) notation' #<-------
'what built ins are imutable and mutable'#<-------
'tuples are immutable so they would be stored in a set'
'polymorphism encapsulation composition inheritance'
'objects in python'
'phases in test driven development'#<-------
'Red: write a test run to verify it fails Yellow: Green: '
'some nested for lops are not n^2'
'what is a wrapper pattern: use compostion to implement some public interface with an existing data structure that already gives us that functionality'
'Wrappers' #<-------
'difference between stack and queue: stack is FILO queue is FIFO'
'difference between linked list and doubly list: LL is slow if you need to traverse backwards LL only allows you to go one direction'
'Errors in python and how to write them ex.TypeError' #<------
def foo(x):
    x = 3
    return x

x = "hello"
print(foo(x))