'''list are mutable because we can change what in the list is bound to 
lists, sets, and dictionaries are all mutable and 
lists, tuples, and strings have a fixed order.'''

l= ['a','b','c','d','e']
#slice the list (can do with tuples and string as well)
print(l[0:3])
#iteration
for item in l:
    print(item)
